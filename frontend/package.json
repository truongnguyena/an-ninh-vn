{
  "name": "frontend",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host",
    "build": "vite build",
    "preview": "vite preview --host",
    "check": "vite build --mode development"
  },
  "devDependencies": {
    "vite": "^5.4.21"
  },
  "dependencies": {
    "socket.io-client": "^4.8.3"
  }
}
